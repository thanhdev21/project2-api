import { createComment } from './create';
import { updateComment } from './update';
import { deleteComment } from './delete';

export default {
  createComment,
  updateComment,
  deleteComment,
};
